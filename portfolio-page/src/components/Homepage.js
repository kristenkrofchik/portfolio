import React from 'react';
import Layout from '../shared/Layout';
import Hero from './Hero';

const Homepage = () => {
    return (
        <Layout>
            <Hero />
        </Layout>
    )
};

export default Homepage;