import React from 'react';
import Layout from '../shared/Layout';

const Connect = () => {
    return (
        <Layout>
            <h1>Connect with Me</h1>
        </Layout>
    )
};

export default Connect;